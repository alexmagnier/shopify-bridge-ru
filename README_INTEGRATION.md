# 🎯 ИНСТРУКЦИЯ ПО ИНТЕГРАЦИИ SUPABASE

## ✅ ЧТО УЖЕ СКОПИРОВАНО:

1. ✅ `src/integrations/supabase/client.ts` - Клиент Supabase
2. ✅ `src/integrations/supabase/types.ts` - Типы базы данных
3. ✅ `src/contexts/AuthContext.tsx` - Контекст авторизации
4. ✅ `src/utils/leadSubmission.ts` - Отправка лидов (уже было)

---

## 🔧 ЧТО НУЖНО СДЕЛАТЬ:

### 1️⃣ Установить зависимости

```bash
npm install @supabase/supabase-js
```

### 2️⃣ Создать файл `.env.local`

Создай файл `.env.local` в корне проекта:

```bash
VITE_SUPABASE_URL=https://oyjxzrvhvndbdoyshwfc.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=твой_ключ_здесь
```

**Где взять ключ:**
1. Открой https://supabase.com/dashboard
2. Выбери свой проект
3. Settings → API
4. Скопируй `anon public` ключ

### 3️⃣ Обновить `vite.config.ts`

Добавь алиас `@`:

```typescript
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import path from 'path';

export default defineConfig({
  plugins: [react()],
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
    },
  },
});
```

### 4️⃣ Обновить `tsconfig.json`

Добавь пути:

```json
{
  "compilerOptions": {
    "paths": {
      "@/*": ["./src/*"]
    }
  }
}
```

### 5️⃣ Обернуть App в AuthProvider

В `src/main.tsx`:

```tsx
import { AuthProvider } from './contexts/AuthContext';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <AuthProvider>
      <App />
    </AuthProvider>
  </React.StrictMode>
);
```

---

## 🧪 ТЕСТИРОВАНИЕ:

### Проверь что всё работает:

1. **Открой консоль:**
   ```bash
   npm run dev
   ```

2. **Проверь что нет ошибок импорта**

3. **Открой страницу `/partners/register`**
   - Должна открыться форма регистрации
   - Заполни и отправь
   - Проверь email для подтверждения

4. **Открой Supabase Dashboard**
   - Table Editor → `partners`
   - Должна появиться новая запись

---

## 📝 СЛЕДУЮЩИЕ ШАГИ:

Когда всё заработает, напиши мне **"Суп аbase подключен"** и я:
- Обновлю партнерские страницы для работы с реальными данными
- Подключу реальную загрузку рефералов
- Настрою выплаты

---

## ❓ ПРОБЛЕМЫ:

### Ошибка: "Cannot find module '@/integrations/supabase/client'"

**Решение:** Проверь что:
1. Путь `@` настроен в `vite.config.ts`
2. Файл `client.ts` существует
3. Перезапусти dev сервер

### Ошибка: "Invalid Supabase URL"

**Решение:** Проверь `.env.local`:
1. Файл создан в корне проекта
2. Переменные начинаются с `VITE_`
3. URL правильный

---

## 🎉 ГОТОВО!

После выполнения всех шагов у тебя будет:
✅ Подключение к Supabase
✅ Авторизация партнеров
✅ Отправка лидов через Edge Function
✅ Реальная база данных

**Жду сообщения "Готово"!** 🚀

